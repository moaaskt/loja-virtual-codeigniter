# Integrations

## Internal Systems
- **Database**: Local MySQL 5.7 instance running within Docker (`loja_db`). The database is named `loja_virtual` with user `user_loja`.

## External Services
- No explicit third-party API integrations (e.g., payment gateways or email providers) are currently documented or present in the core structure. Payments and emails are likely mocked or handled internally for this study project.
